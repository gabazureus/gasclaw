node_modules/
dist/
.vitest/
.env.local
.clasprc.json
.clasp.json
*.log
.DS_Store
.env
.tmp/

# Identificadores do ambiente (conta, projetos GCP, script ids, deployment ids).
# Fica fora do versionamento: o repositório é publicável (MIT, ADR-030) e estes valores são do dono.
# Use gasclaw.env.example como referência dos nomes.
gasclaw.env
