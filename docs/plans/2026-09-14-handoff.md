# Handoff — gasclaw (2026-09-14)

> Leia este arquivo inteiro antes de qualquer ação. Ele substitui a conversa anterior.

## 1. Onde estamos
- **Design aprovado** pelo usuário: `docs/specs/2026-09-14-gasclaw-design.md` + ADRs 001–008 (`docs/adr/`).
- **Plano de implementação pronto e revisado:** `docs/plans/2026-09-14-gasclaw-f0-plano-implementacao.md`
  (Parte A = histórico completo de decisões; Parte C = Tasks 0–11 com código; Parte D = F1–F4; Parte E = checklist).
- **Nenhuma linha de código escrita. Nenhum commit** (`git init -b main` feito). Próximo passo = **Task 0**.

## 2. Como trabalhar (pedido explícito do usuário)
1. **Usar `/devmode lean`** e o **devmode-orchestrator** para executar o plano. O comando `/devmode`
   só existe quando o Claude Code é aberto **com diretório de trabalho `~/Projects/gasclaw`**
   (arquivo `.claude/commands/devmode.md` instalado pelo adopt). Na sessão anterior (cwd `testes`) deu
   "Unknown command". O hook `devmode_phase_gate.py` bloqueia turnos sem o orchestrator
   (escape: `DEVMODE-OK: <motivo>`).
2. **Minimal code** (`.claude/skills/minimal-code`, intensidade `full`): escada antes de escrever;
   atalhos com `// minimal:`; nunca cortar validação, erros ou segurança.
3. **Pedir ao usuário tudo que for necessário** via AskUserQuestion (decisões, confirmações). Não
   presumir. Idioma: **pt-BR**.
4. **Mostrar o passo a passo** do andamento (task atual, resultado, próximo).
5. **Zero operação manual depois do setup**: tudo via `./gasclaw <comando>`.
6. Beads está ativo em modo stealth (`bd`) — use para handoffs/tarefas se o orchestrator pedir.

## 3. O que depende do usuário (pedir no momento certo, nunca digitar credenciais)
- Tasks 0–8: **nada** (rodam sozinhas). Confirmar antes do primeiro commit (Task 0).
- Task 9 (`./gasclaw up`): logins gcloud/clasp no navegador; ligar Apps Script API; tela de consentimento
  INTERNA; vincular número do projeto GCP no script; autorizar web app; configurar Chat app;
  colar chave `sk-or-…` (OpenRouter) no `.env.local` e na tela; URL da pasta do agente.
- Task 10: **confirmar antes** de criar repo privado no GitHub, publicar secret e dar push.

## 4. Estado do ambiente (verificar de novo)
- Node v24.1.0, git, python3, Homebrew 6.0.22, `bd` 1.2.2: OK.
- **`gcloud` instalado** (Google Cloud SDK 584.0.0, em `/usr/local/share/google-cloud-sdk/bin` — o `gasclaw` já
  adiciona ao PATH). **`clasp` 3.4.1 global instalado** (o plano ainda usa o clasp local via `npm install -D`).
  `gh` não instalado (o `./gasclaw up` instala). Nenhum login feito ainda.
- Versões confirmadas no npm: esbuild 0.28.2, vitest 5.0.0, @types/google-apps-script 2.0.13; clasp 3.4.1.

## 5. Decisões-chave (resumo; detalhes na Parte A do plano)
- Runtime 100% Google Apps Script; agente = pasta do Drive (só markdown, sem `eval`).
- Canal principal Google Chat (app clássico, resposta síncrona na F0).
- **LLM: só OpenRouter** (sem Gemini/Vertex).
- GASADK só se a POC P5 passar (F2); F0 usa turno simples.
- Dono do script/triggers = conta do usuário; dev + prod; CI no push da `main`.
- `docs/` é o hub; `conductor/` na raiz.
- Web app `access: MYSELF` (fallback `DOMAIN` se o Chat exigir — Task 9, Step 6).

## 6. Pontos de atenção já conhecidos
- Conferir flags do clasp 3.4.1 antes do primeiro deploy (Task 8, Step 4).
- `gcloud projects create` pode exigir papel Project Creator na organização.
- Chat tem limite de 30 s; se o modelo for lento, trocar `model` no `AGENTS.md`.
- Após `/devmode update`, refazer a troca de caminhos do wiki (`docs/runbooks/devmode-update.md`).

## 7. Prompt para iniciar a nova conversa
Abrir o Claude Code em `~/Projects/gasclaw` e colar:

```
/devmode lean Execute o plano docs/plans/2026-09-14-gasclaw-f0-plano-implementacao.md a partir da Task 0. Leia antes docs/plans/2026-09-14-handoff.md. Use o devmode-orchestrator e minimal-code (full), me pergunte tudo via AskUserQuestion, mostre o passo a passo e peça confirmação antes de commits, criação de recursos no Google/GitHub e push.
```
