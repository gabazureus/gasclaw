# Tracks

> Master list of work tracks (features, bugs, improvements). Each track lives in
> `conductor/tracks/<track_id>/` with its `spec.md`, `plan.md`, and `learnings.md`.
> Create new tracks with `/conductor-newtrack` (run `grill-me` first to reach the
> design concept). Status: `[ ]` pending · `[~]` in progress · `[x]` done · `[!]` blocked.

<!-- Tracks are appended below this line by /conductor-newtrack -->

---

<!-- Example shape (delete once you have real tracks):

## [ ] Track: <short description>
- Folder: `conductor/tracks/<track_id>/`
- Beads epic: `bd-xxxx` (if enabled)

-->
