# Plano — F8

1. Fase 1a — núcleo: `inheritable` (lista fechada, segredos barrados) — teste antes, mutação.
2. Fase 1b — porta `inherit` no sucessor + `crownSuccessor` herda antes de ligar + `succession inherit <id>`.
3. Fase 1c — health: 10ª checagem (permissões), health funciona em coroado, código comparado ao build atual.
4. Publicar: `./gasclaw up` → `./gasclaw succession sync <id>` → `./gasclaw succession inherit <id>` → health.
5. Fase 2 — P35 (portão humano: 1 mensagem no Chat) → roteamento `chatrelay` ou ADR do caminho manual.
6. Fase 3 — G (causa-raiz) → F (apagar fila órfã) → I (checklist, sem ship).
7. Docs + commit + push a cada fase.
