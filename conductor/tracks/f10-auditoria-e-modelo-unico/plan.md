# Plano — F10

1. Auditoria do diff `e4b8006..HEAD` (segurança + corretude); lista de achados com causa-raiz.
2. P1 (tique ocioso > 1000 ms) e P2 (chamada de modelo a mais por aprovação) medidos no dev, N ≥ 10.
3. Consertos test-first + mutação; suíte e tsc verdes; commit.
4. Cobertura ferramenta → eval; escrever os evals que faltam; `./gasclaw eval --all` no motor que responde.
5. Provar o id `gpt-6-luna`; decidir com o dono o juiz e o escritor do sucessor; aplicar a troca; medir custo.
6. Publicar no dev, health 10/10, docs (ADR da troca), commit e push.
